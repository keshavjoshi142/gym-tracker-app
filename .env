# Default Environment Variables
# Note: Specific environment files (.env.development, .env.production) take precedence
# Only put shared/default values here that should be overridden by specific environments